У меня неправильный файл с QC для старого аффисека!!!



- выкинуть ZNF997, он не нужен


- устранить ошибку с аффисек-ридами

- Попросить Ивана обсчитать датасеты:
find /mnt/space/hughes/June1st2021/SELEX_RawData/Phase3 -xtype f | grep Aff

- найти все исходники всех датасетов на сервере и сделать симлинки на них. Попросить больше НИКОГДА не менять их, не оставляя симлинки
- найти чипсеки, которые уже обсчитаны. И подготовить по ним датасеты для мотив-дискавери
- сделать PBM Train/Val обмен, чтобы обсчитать мотивы на нынешних val-датасетах



В папке AFS (и в таблицах BioUML) есть файл, которого нет в мете:
	ZNF997_AffSeq_Lysate_BatchAATA


В папке HTS есть файлы, для которых нет меты:
	ZNF280A_TC40NGTTTTG_IVT_BatchAATBA
	ZNF997_TA40NGTTAGC_Lysate_BatchAATA

Разные циклы в разных батчах:
	ZBED2_AC40NTCCTTG_IVT_BatchYWCA1
	ZBED2_AC40NTCCTTG_IVT_BatchYWCA2


In `TFGenes` cells of columns "Present in * sheet" were replaced with true/false.


In `Plasmids`:
- pTH15990 --> pTH15883 (row id=889).
- Column J (Plasmid name) should be removed as a duplicate for column B.
- Columns R, W have no heading. Should be either removed or named
- Column S,T,U,V are empty. Should be removed

In `Inserts`:
- (NOT YET APPLIED) Plasmid name eGFP.OCT4/POU5F1 -> eGFP.POU5F1, GST.OCT4/POU5F1 -> GST.POU5F1
- empty columns
- the last column `DBD type` is duplicated


CHS:
- THC_0272 and THC_0277 are missing. Is that ok?
- What is going on with THC_0231	[-pTH13799	TIGD7	pTH13799.1.2-]{+pTH13635	PRDM13	pTH13635.1.2+}	PRDM13ChIP2	ChIP	2		PRDM13ChIP2_IGO_10521_47_S111_R2_001.fastq.gz	MSK	{+3+}
- (NOT YET APPLIED) added column `Batch`


в AFS и HTS дохрена новых данных. В CHS есть изменившийся датасет (см.выше)

